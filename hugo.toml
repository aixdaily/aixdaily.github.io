baseURL = 'https://aixdaily.github.io/'
languageCode = 'en-us'
title = 'My New Hugo Site'
theme = 'dream'

# Add these if not present
[module]
  [module.hugoVersion]
    extended = true
    min = "0.87.0"